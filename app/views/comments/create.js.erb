$(".new_comment").replaceWith("<div class='new_comment'><%= j nested_comments @post.comments.arrange(order: :created_at)%>")
$(".reply_comment_form, .no_comments_message").hide(500);
$('.new_comment_form')[0].reset();